package pack1;
public class PArent {
	protected void show() {
		System.out.println("parent");
	}
	//public working in Child class
	// protected keyword is used for exclusivity of use with Child class and only child extended to parent can access it
}
